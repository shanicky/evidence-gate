interface:
  display_name: "Decision Assurance"
  short_description: "Route strong claims through stakes, evidence, and action governance"
  default_prompt: "Use $decision-assurance to classify the stakes, judge the explicit evidence, and return the governed action for this claim or recommendation."
policy:
  allow_implicit_invocation: true
